
name: 'dim_company_members'
version: '0.1'
profile: 'user_interviews'
source-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
data-paths: ["data"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
    - "target"
    - "dbt_modules"
    - "logs"

models:
  dim_company_members:
      materialized: table
      staging:
        materialized: view
        tags: ["staging", "hourly"]
